def u32 = #primitives.u32 // Comment test
def bool = /* comment test */ #primitives.bool

def print_bigger = func(a : u32, b : u32) do
	#output.print_format("Hurra! Ditt nummer, {}, är större än {}", a, b)
end

def print_smaller = func(a : u32, b : u32) do
	#output.print_format("Buu. Ditt nummer, {}, är mindre än {}", a, b)
end

def print_same = func(a : u32, b : u32) do
	#output.print_format("Va? Ditt nummer är ju {}", a)
end

var a = 2 * (123 + 2 - 1) / 3
var b = if a > 0 => 42 else 0

if a > b then
	print_bigger(a, b)
elseif a < b then
	print_smaller(a, b)
else
	print_same(a, b)
end