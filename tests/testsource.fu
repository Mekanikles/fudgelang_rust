def u32 = #primitives.u32 // Comment test

def add = func(a : u32, b : u32) -> u32 do
	return a + b
end

def sub = func(a : u32, b : u32) -> u32 do
	return a - b
end

def mul = func(a : u32, b : u32) -> u32 do
	return a * b
end

def div = func(a : u32, b : u32) -> u32 do
	return a / b
end

def print = /* comment test */ func(a : u32) do
	#output.print_format("Ditt nummer är: {}", a)
end

print(div(mul(2, sub(add(123, 2), 1)), 3))
